# @react-three/fiber@9.7 déclare un intervalle de peer trop strict (react ">=19 <19.3") qui
# exclut React 19.3.0 lui-même — un décalage de plage, pas une vraie incompatibilité (T8, vue 3D).
# Sans ce réglage, `npm install` sur un clone neuf échoue avec un conflit ERESOLVE.
legacy-peer-deps=true
